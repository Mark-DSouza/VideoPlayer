import React, {Fragment} from 'react';

import ReactPlayer from 'react-player';

const Video = props => {
    return (
        <div>
            Video!!!
        </div>
    );
}

export default Video;